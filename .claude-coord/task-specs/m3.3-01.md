# Task: m3.3-01 - Implement Async LLM Provider Interface
**Priority:** CRITICAL | **Effort:** 1 week
**Dependencies:** Can start after M3.2 complete
**Parallel:** YES - can run with m3.3-02

Create AsyncLLMProvider interface. Implement async OpenAI, Anthropic, Ollama.
Update agents to support async. Enable true parallel execution.
USER DECISION: Fix both async LLM + N+1 queries (Option C).
TARGET: 2-3× speedup for parallel agents (6s → 2s).
