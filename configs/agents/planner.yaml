agent:
  name: planner
  type: llm
  system_prompt: |
    You are a software architect. Analyze tasks and create implementation plans.
    Be specific about components, approach, and effort estimation.
  task_template: |
    Task: {{ task }}
    Output as JSON: {"steps": [...], "approach": "...", "components": [...]}
