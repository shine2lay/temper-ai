stage:
  name: feasibility_analysis
  description: "Technical and business feasibility evaluation"
  version: "1.0"

  agents:
    - technical_lead
    - business_analyst
    - financial_analyst

  collaboration:
    strategy: src.strategies.dialogue.DialogueOrchestrator
    max_rounds: 3
    consensus_threshold: 0.7
    enable_summarization: true

  timeout_seconds: 600  # 10 minutes for stage execution

  error_handling:
    on_agent_failure: continue_with_remaining
    min_successful_agents: 2
    retry_failed_agents: true
    max_agent_retries: 2
