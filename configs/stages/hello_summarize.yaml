stage:
  name: hello_summarize
  description: "Summarize the analysis"
  version: "1.0"

  agents:
    - hello_summarizer

  execution:
    agent_mode: sequential
    timeout_seconds: 300

  outputs:
    summary:
      type: string
      description: "Brief summary of the analysis"
