stage:
  name: vcs_spec_decision
  description: "Final spec decision based on perspectives"
  version: "1.0"

  agents:
    - vcs_spec_decider

  execution:
    agent_mode: sequential
    timeout_seconds: 600

  collaboration:
    strategy: sequential
    max_rounds: 1
    convergence_threshold: 0.8

  conflict_resolution:
    strategy: highest_confidence

  inputs:
    suggestion_text:
      type: string
      required: true

  outputs:
    spec_output:
      type: string
      description: "Final implementation spec"

  safety:
    mode: execute
    risk_level: low
    require_approval: false

  tags:
    - vcs
    - spec
    - decision
