# Example custom tool definition
tool:
  name: WebSearch
  description: Search the web using SearXNG. Returns top results with titles, URLs, and snippets.
  parameters:
    type: object
    properties:
      query:
        type: string
        description: The search query
    required:
      - query
