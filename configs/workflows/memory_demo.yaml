workflow:
  name: memory_demo
  description: "Demonstrates agent memory — run twice to see memories from the first run appear in the second"
  version: "1.0"
  inputs:
    required:
      - topic
    optional:
      - context
  stages:
    - name: research
      stage_ref: configs/stages/memory_research_stage.yaml
      description: "Research stage using agent with persistent memory"
  error_handling:
    on_stage_failure: halt
    max_stage_retries: 1
    escalation_policy: LogAndContinue
  outputs:
    final_stage: research
