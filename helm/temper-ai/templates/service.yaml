apiVersion: v1
kind: Service
metadata:
  name: {{ include "temper-ai.fullname" . }}
  labels:
    {{- include "temper-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "temper-ai.selectorLabels" . | nindent 4 }}
